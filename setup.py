from setuptools import find_packages, setup

setup(
    name='lora_model',
    description='lora model that combines protein and molecule foudation models.',
    author='Grant McConachie',
    packages=find_packages()
)